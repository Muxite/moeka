# Tool Usage Notes

Tool signatures are provided automatically via function calling.
This file documents non-obvious constraints and usage patterns specific to this agent.

Edit to record host-specific tool conventions (sudo policy, disk operation rules,
internal-state files to never touch directly, etc.).
